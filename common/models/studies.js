'use strict';
//var BriefSummaries = require('./brief-summaries')
module.exports = function(Studies) {
  //  Studies.embedsMany(BriefSummaries, {
  //      as: 'briefSummaries', // default to the relation name - emailAddresses
  //      property: 'briefSummaries' // default to emailAddressItems
  //      });
};
